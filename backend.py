#!/bin/python

#if not MAIDSAFE
	#Generate random id
	#store random_id in var
	#display random_id in HTML for user to share with others
#if MAIDSAFE
	#Figure out how to pull API id. 
	#assign that user access to shared storage
	#create temporary storage token. 
	#look for other tokens and findfriends()
	#synchronize storage with all clients
	